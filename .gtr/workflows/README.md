# Workflows locaux

Les slash-commands GTR sont centralisées :

`D:\GrokBuild\Agents\.agents\workflows\`

Exemples : `/create-feature`, `/security-audit`, `/release-master`, `/squad-brainstorm`.

Ajouter ici uniquement des workflows **spécifiques** à ce projet (ex. script métier unique).
